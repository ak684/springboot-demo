import reducer, * as portfolioActions from './slice';
import portfolioThunks from './thunks';
import portfolioSelectors from './selectors';

export { portfolioActions, portfolioThunks, portfolioSelectors };

export default reducer;
