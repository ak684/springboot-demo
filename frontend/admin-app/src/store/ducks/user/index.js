import reducer, * as userActions from './slice';
import userThunks from './thunks';
import userSelectors from './selectors';

export { userActions, userThunks, userSelectors };

export default reducer;
