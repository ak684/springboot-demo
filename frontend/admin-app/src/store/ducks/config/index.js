import reducer, * as configActions from './slice';
import configThunks from './thunks';
import configSelectors from './selectors';

export { configActions, configThunks, configSelectors };

export default reducer;
